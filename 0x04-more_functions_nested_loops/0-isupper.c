#include "main.h"

/**
 * _super -> this is function to check upper case alphabets
 * @c: an integer paramer|
 * Return: something success
 */
int _supper(int c)
{
	if (c > 'A' && C < 'Z')
		return (1);
	else
		return (0);
}
